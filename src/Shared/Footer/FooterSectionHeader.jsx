import React from "react";

const FooterSectionHeader = ({ children }) => {
  return (
    <h2 className="text-[14px] font-semibold font-lato text-[#064532] mb-6">
      {children}
    </h2>
  );
};

export default FooterSectionHeader;
